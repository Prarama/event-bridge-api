# event-bridge-api
A robust RESTful API backend for a virtual event platform built with Node.js and Express. Features JWT auth, bcrypt hashing, and role-based access for Organizers and Attendees. Uses in-memory storage for event CRUD, participant tracking, and async email alerts on registration
